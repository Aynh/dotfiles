--merge-output-format mkv
--output "[%(upload_date)s] %(title)s (%(id)s).%(ext)s"

--embed-subs
--sub-langs all,-live_chat

--embed-info-json
--embed-chapters
--embed-thumbnail
--embed-metadata

--force-ipv4
--downloader aria2c
--downloader-args 'aria2c:--split=48 --max-connection-per-server=48 --min-split-size=1M'

--playlist-random
# --lazy-playlist

--geo-bypass
--geo-bypass-country JP
